# files - List package's files

```bash
napm files <package>
napm files --dirs <package>
```

By default only files are listed. Pass `--dirs` to also include directories. Requires the file cache (run `napm update --files`).
